[tool.black]
line-length = 100
extend-exclude = '''
^/breathe/parser/.*
'''

[build-system]
requires = [
    "setuptools>=45",
    "wheel",
    "setuptools_scm>=6.2",
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "breathe/version.py"
